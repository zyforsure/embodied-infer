# Mock model

Deterministic dependency-free backend used for protocol, scheduler, and
simulator integration tests. It implements the same `ModelSpec` and
`ModelBackend` contracts as accelerator-backed models.
